<div class="page-header">
  <h1 class="text-center"><%= @project.name.upcase %> Git Repositories</h1>
</div>
<div class="row-fluid">
  <div class="span3">
    <%= render :partial => 'sidebar' %>
  </div>
  <div class="span9">
  <% @project.array_repos.each do |repo| %>
    <div class="well well-small">
      <div class="btn-group">
	<button class="btn"><%= t :git %></button>
	<input type="text" name="country" value="Norway" readonly>
      </div>
    </div>
  <% end %>
    <table class="table table-striped">
      <thead>
	<tr>
	  <th><%= t :id %></th>
	  <th><%= t :title %></th>
	  <th><%= t :author %></th>
	  <th><%= t :date_time %></th>
	  <th><%= t :files_changed %></th>
	</tr>
      </thead>
      <tbody>
	<% @git.each do |commit| %>
	  <tr>
	    <td><a href="<%= commit.id %>"><%= commit.id[commit.id.index('h=')+2..commit.id.index('h=')+9] %></a></td>
	    <td><%= commit.title %></td>
	    <td><%= commit.author %></td>
	    <td><%= commit.published.to_s[0..18] %></td>
	    <td><%= commit.content.scan("\n").length-8 %></td>
	  </tr>
	<% end %>
      </tbody>
    </table>
  </div>
</div>