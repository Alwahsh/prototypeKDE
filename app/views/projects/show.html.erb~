<%- model_class = Project -%>
<div class="page-header">
  <h1 class="text-center"> Project: <%= @project.name.upcase %></h1>
</div>

<div class="row-fluid">
  <div class="span3">
    <div class="well sidebar-nav">
            <ul class="nav nav-list">
              <li class="nav-header"><%= @project.name %></li>
              <li><%= link_to "Bug Report", bug_project_path %></li>
              <li class="disabled"><%= link_to "Git Report", git_project_path %></li>
              <li class="disabled"><%= link_to "Maillists Report", mailList_project_path %></li>
              <li class="disabled"><%= link_to "IRC Channels Report", ircChannel_project_path %></li>
            </ul>
    </div><!--/.well -->
  </div>
  <div class="span9">
    <div class="well well-small">
      <h3>Bugs:</h3>
      <table class="table table-striped">
	<thead>
	  <tr>
	    <th class="bugsTableHead">Confirmed</th>
	    <th class="bugsTableHead">Unconfirmed</th>
	    <th class="bugsTableHead">Assigned</th>
	    <th class="bugsTableHead">Reopened</th>
	    <th class="bugsTableHead">Resolved</th>
	    <th class="bugsTableHead">NeedsInfo</th>
	    <th class="bugsTableHead">Verified</th>
	  </tr>
	</thead>
	<tbody>
	  <tr>
	    <td class="bugsTable"><%= @bugs_by_state["CONFIRMED"].length %></td>
	    <td class="bugsTable"><%= @bugs_by_state["UNCONFIRMED"].length %></td>
	    <td class="bugsTable"><%= @bugs_by_state["ASSIGNED"].length %></td>
	    <td class="bugsTable"><%= @bugs_by_state["REOPENED"].length %></td>
	    <td class="bugsTable"><%= @bugs_by_state["RESOLVED"].length %></td>
	    <td class="bugsTable"><%= @bugs_by_state["NEEDSINFO"].length %></td>
	    <td class="bugsTable"><%= @bugs_by_state["VERIFIED"].length %></td>
	  </tr>
	</tbody>
      </table>
    </div>
    <div class = "well well-small">
      <h3>Git:</h3>
      <p>Under Construction.</p>
    </div>
    <div class = "well well-small">
      <h3>Mail Lists:</h3>
      <p>Under Construction.</p>
    </div>
    <div class = "well well-small">
      <h3>IRC Channels:</h3>
      <p>Under Construction.</p>
    </div>
      <div class="form-actions">
	<%= link_to t('.back', :default => t("helpers.links.back")),
		    projects_path, :class => 'btn'  %>
	<%= link_to t('.edit', :default => t("helpers.links.edit")),
		    edit_project_path(@project), :class => 'btn' %>
	<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
		    project_path(@project),
		    :method => 'delete',
		    :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
		    :class => 'btn btn-danger' %>
      </div>
  </div>
</div>